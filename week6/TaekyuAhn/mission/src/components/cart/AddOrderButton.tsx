import React from "react";
import styles from "./AddOrderButton.module.scss";

const AddOrderButton = () => {
  return <button className={styles.button}>더 담기 +</button>;
};

export default AddOrderButton;
